# hugo.yaml for Hugo module 'lightboxSSA'

module:
  # Need at least 64 for ?  and extended for SASS
  hugoVersion:
    min: 0.64.0
    extended: true
  mounts:
    - source: src/partials
      target: layouts
    - source: src
      target: assets
